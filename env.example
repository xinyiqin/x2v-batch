# S2V API 配置
LIGHTX2V_BASE_URL=https://x2v.light-ai.top
LIGHTX2V_ACCESS_TOKEN=your_access_token_here

# 存储配置
# 选项: local (本地存储) 或 s3 (S3 对象存储)
STORAGE_TYPE=local

# 本地存储配置（当 STORAGE_TYPE=local 时）
DATA_DIR=./data

# S3 存储配置（当 STORAGE_TYPE=s3 时）
# S3_CONFIG 是一个 JSON 字符串，包含以下字段：
# {
#   "aws_access_key_id": "your_access_key_id",
#   "aws_secret_access_key": "your_secret_access_key",
#   "endpoint_url": "https://s3.amazonaws.com",
#   "bucket_name": "your-bucket-name",
#   "base_path": "x2v-batch",
#   "region": "us-east-1",
#   "addressing_style": "virtual",  # 火山引擎 TOS 需要此参数
#   "cdn_url": ""  # 可选，CDN URL
# }
# S3_CONFIG={"aws_access_key_id":"xxx","aws_secret_access_key":"xxx","endpoint_url":"https://s3.amazonaws.com","bucket_name":"xxx","base_path":"x2v-batch","addressing_style":"virtual"}

# 前端 API 地址（用于构建时注入）
# 如果使用 Nginx 反向代理，设置为相对路径或空（使用 /api）
# 如果直接访问后端，设置为后端地址，如 http://your-domain.com:8000
VITE_API_BASE=

# 可选：数据库配置（如果使用数据库）
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# 可选：Redis 配置（如果使用 Redis）
# REDIS_URL=redis://localhost:6379/0

# 应用配置
# ENVIRONMENT=production
# DEBUG=false

# 管理员初始密码（首次创建 admin 用户时使用）
# 如果不设置，默认使用 admin8888
ADMIN_PASSWORD=admin8888

